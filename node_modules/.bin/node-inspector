#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../node-inspector/bin/inspector.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../node-inspector/bin/inspector.js" "$@"
  ret=$?
fi
exit $ret
